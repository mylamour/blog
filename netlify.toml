[build]
  publish = "public"
  command = "node themes/fexo2/rev.js && hexo generate"

# 压缩由本地构建链完成（dart-sass/esbuild/minify-html.js/minify-css.js），
# 关闭 Netlify 后处理避免它改写带 ?v= 的资源引用。
# 缓存头统一由 source/_headers 管理。
[build.processing]
  skip_processing = true
